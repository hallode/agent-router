{
  "version": 1,
  "enforce": true,
  "bypass_prefix": "!!",
  "degrade": {
    "NORMAL": {
      "trivial": "haiku",
      "execution": "sonnet",
      "reasoning": "opus"
    },
    "CONSERVE": {
      "trivial": "haiku",
      "execution": "sonnet",
      "reasoning": "sonnet"
    },
    "CRITICAL": {
      "trivial": "haiku",
      "execution": "haiku",
      "reasoning": "sonnet"
    },
    "DEPLETED": {
      "trivial": "haiku",
      "execution": "haiku",
      "reasoning": "haiku"
    }
  },
  "effort": {
    "execution": "medium",
    "reasoning": "high"
  },
  "effort_comment": "Claude effort per task tier, passed as --effort on a prompted launch and shown with /model suggestions. An unmapped tier (trivial here) passes no flag, so the host default applies. Subagent effort is set by each agent's own `effort:` frontmatter; the Agent tool cannot carry it.",
  "compact": {
    "threshold": 160000,
    "threshold_1m": 250000,
    "repeat_every": 60000,
    "comment": "Claude advisor suggests /compact once the latest turn's context passes the threshold (threshold_1m for [1m] models), then again after each repeat_every tokens of growth."
  },
  "agent_type_tiers": {
    "statusline-setup": "trivial",
    "Explore": "execution",
    "Plan": "reasoning",
    "claude-code-guide": "execution",
    "router-scout": "trivial",
    "router-builder": "execution",
    "router-inspector": "reasoning",
    "router-navigator": "reasoning"
  },
  "agent_type_tiers_comment": "Map any agent type to a tier when its prompts are too varied to classify. The router's own roles (scout, builder, inspector, navigator) route without an entry; list them only to change their tier.",
  "codex": {
    "comment": "Codex settings. chains give each tier its own model, not one model at three efforts: a small model thinking hard is rarely the same trade as a large one thinking briefly. List what your account offers with `jq -r '.models[].slug' ~/.codex/models_cache.json`. allowed_accounts is empty by default, which means cxr never refuses; populate it only if workspaces.enabled is true.",
    "allowed_accounts": []
  },
  "codex_chains": {
    "reasoning": [
      {
        "model": "<your-strongest>",
        "effort": "high"
      },
      {
        "model": "<your-second>",
        "effort": "high"
      },
      {
        "model": "<your-reserve>",
        "effort": "high"
      }
    ],
    "execution": [
      {
        "model": "<your-mid-tier>",
        "effort": "medium"
      },
      {
        "model": "<your-other-mid>",
        "effort": "medium"
      },
      {
        "model": "<your-reserve>",
        "effort": "medium"
      }
    ],
    "trivial": [
      {
        "model": "<your-smallest>",
        "effort": "low"
      },
      {
        "model": "<your-older-small>",
        "effort": "low"
      },
      {
        "model": "<your-reserve>",
        "effort": "low"
      }
    ]
  },
  "workspaces": {
    "enabled": false,
    "comment": "Optional. Only useful if you hold more than one subscription and care which one a directory bills. Labels are arbitrary; the router only compares them.",
    "rules": [
      {
        "prefix": "$HOME/src/oss",
        "account": "personal"
      },
      {
        "prefix": "$HOME/src/acme",
        "account": "employer"
      }
    ],
    "default": "",
    "remote_patterns": [
      {
        "pattern": "git\\.acme\\.example",
        "account": "employer"
      }
    ]
  },
  "quota": {
    "comment": "codex-quota reads Codex's own rate-limit records. Replace with any command printing JSON if your host exposes quota differently; empty disables the probe.",
    "command": "$ROUTER_HOME/bin/codex-quota",
    "percent_jq": "[.. | objects | (.usedPercent // .used_percent // empty)] | max",
    "conserve_at": 60,
    "critical_at": 85,
    "depleted_at": 95,
    "ttl_seconds": 900
  },
  "advisor": {
    "comment": "delegate=false silences the nudge that asks an expensive session to hand bounded work to a subagent.",
    "delegate": true
  }
}
